function Flashcard(options) {
	// Named function
	
}

Flashcard.prototype.language = function(str_lang) {
	// Set base language
	this.language = str_lang;
};

Flashcard.prototype.characterSet = function(str_char) {
	
};

Flashcard.prototype.card = {
	// Card object to be instantiated
		
};



